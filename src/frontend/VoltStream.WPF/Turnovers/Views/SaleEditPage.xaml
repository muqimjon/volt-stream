<Page x:Class="VoltStream.WPF.Sales.Views.SaleEditPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:VoltStream.WPF.Commons.UserControls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:loc="clr-namespace:VoltStream.WPF.Commons.Localization"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utils="clr-namespace:VoltStream.WPF.Commons.Utils"
        Title="{loc:Loc Turnovers.SaleEditTitle}"
        Background="{DynamicResource Canvas}"
        d:DesignHeight="450"
        d:DesignWidth="800"
        mc:Ignorable="d">

	<Grid>
		<Border Style="{StaticResource CardBorder}" Margin="10">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="150" />
					<RowDefinition Height="30" />
					<RowDefinition Height="*" />
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" MaxWidth="500" />
						<ColumnDefinition Width="20" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="80" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="*" />
							<RowDefinition Height="*" />
							<RowDefinition Height="*" />
							<RowDefinition Height="*" />
							<RowDefinition Height="*" />
							<RowDefinition Height="6" />
						</Grid.RowDefinitions>

						<Label Content="{loc:Loc Turnovers.DateLabel}" FontSize="14" VerticalAlignment="Center" />
						<Label Grid.Row="1" Content="{loc:Loc Turnovers.CustomerLabel}" FontSize="14" VerticalAlignment="Center" />
						<Label Grid.Row="2" Content="{loc:Loc Turnovers.CurrencyTypeLabel}" FontSize="14" VerticalAlignment="Center" />
						<Label Grid.Row="3" Content="{loc:Loc Turnovers.DiscountLabel}" FontSize="14" VerticalAlignment="Center" />
						<Label Grid.Row="4" Content="{loc:Loc Turnovers.NoteLabel}" FontSize="14" VerticalAlignment="Center" />

						<controls:UserCalendar  x:Name="calendar"
                    Grid.Column="1"
                    Width="150"
                    Margin="0,0,10,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="14"
                    SelectedDate="{Binding Sale.Date}" />

						<ComboBox x:Name="cbxCustomer"
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,2"
                    DisplayMemberPath="Name"
                    FontSize="14"
                    IsEditable="True"
                    Style="{StaticResource RoundedCombo}"
                    ItemsSource="{Binding Customers}"
                    SelectedItem="{Binding SelectedCustomer}"
                    utils:ComboBoxQuickNavBehavior.EnableQuickNavigation="True" />

						<Grid Grid.Row="2" Grid.Column="1">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="90" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<ComboBox x:Name="cbxCurrency"
                        Margin="0,2,10,2"
                        DisplayMemberPath="Name"
                        FontSize="14"
                        IsEditable="False"
                        Style="{StaticResource RoundedCombo}"
                        ItemsSource="{Binding Currencies}"
                        SelectedItem="{Binding SelectedCurrency}" />

							<Label Grid.Column="1" Content="{loc:Loc Turnovers.TotalAmountLabel}" FontSize="14" HorizontalAlignment="Right" VerticalAlignment="Center" />
							<TextBox x:Name="txtSaleTotalAmount"
                        Grid.Column="2"
                        Height="25"
                        Margin="0,2"
                        VerticalAlignment="Top"
                        FontSize="14"
                        IsReadOnly="True"
                        Style="{StaticResource RoundedTextBox}"
                        utils:NumericInput.IsNumeric="True"
                        utils:NumericInput.Value="{Binding TotalSum}" />
						</Grid>

						<Grid Grid.Row="3" Grid.Column="1">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="35" />
								<ColumnDefinition Width="55" />
								<ColumnDefinition Width="90" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<TextBox x:Name="txtSaleTotalDiscount"
                        Margin="0,2,0,2"
                        FontSize="14"
                        IsReadOnly="True"
                        Style="{StaticResource RoundedTextBox}"
                        utils:NumericInput.IsNumeric="True"
                        utils:NumericInput.Value="{Binding Sale.Discount}"/>

							<Border
                        Grid.Column="1"
                        Margin="2,2,0,2"
                        BorderBrush="{DynamicResource BorderStrong}"
                        BorderThickness="0.5">
								<CheckBox x:Name="chkIsDiscountApplied"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="12"
                            IsChecked="{Binding Sale.IsDiscountApplied}" />
							</Border>

							<Label Grid.Column="3" Content="{loc:Loc Turnovers.ToPayLabel}" FontSize="14" HorizontalAlignment="Right" VerticalAlignment="Center" />
							<TextBox x:Name="txtSaleAmount"
                        Grid.Column="4"
                        Margin="0,2"
                        FontSize="14"
                        IsReadOnly="True"
                        Style="{StaticResource RoundedTextBox}"
                        utils:NumericInput.IsNumeric="True"
                        utils:NumericInput.Value="{Binding Sale.Amount}" />
						</Grid>

						<TextBox x:Name="txtSaleDescription"
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="0,2"
                    FontSize="14"
                    Style="{StaticResource RoundedTextBox}"
                    Text="{Binding Sale.Description}" />
					</Grid>

					<Grid Grid.Column="2">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="150" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="*" />
								<RowDefinition Height="*" />
								<RowDefinition Height="*" />
								<RowDefinition Height="*" />
								<RowDefinition Height="40" />
							</Grid.RowDefinitions>
							<TextBlock Text="{loc:Loc Turnovers.AccountLabel}" FontSize="12" FontStyle="Italic" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
							<TextBlock Grid.Row="1" Text="{loc:Loc Turnovers.CurrentLabel}" VerticalAlignment="Center" />
							<TextBox
                        Grid.Row="1"
                        Margin="35,2,2,2"
                        HorizontalContentAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextPrimary}"
                        FontSize="12"
                        IsReadOnly="True"
                        utils:NumericInput.IsNumeric="True"
                        VerticalContentAlignment="Center"
                        utils:NumericInput.Value="{Binding BeginBalance}" />

							<TextBlock Grid.Row="2" Text="{loc:Loc Turnovers.ResultLabel}" VerticalAlignment="Center" />
							<TextBox
                        Grid.Row="2"
                        Margin="35,2,2,2"
                        HorizontalContentAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextPrimary}"
                        FontSize="12"
                        IsReadOnly="True"
                        utils:NumericInput.IsNumeric="True"
                        VerticalContentAlignment="Center"
                        utils:NumericInput.Value="{Binding LastBalance}" />

							<TextBlock Grid.Row="3" Text="{loc:Loc Turnovers.PhoneLabel}" VerticalAlignment="Center" />
							<TextBox
                        Grid.Row="3"
                        Margin="42,2,2,2"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextPrimary}"
                        FontSize="12"
                        IsReadOnly="True"
                        HorizontalContentAlignment="Right"
                        VerticalContentAlignment="Center"
                        Text="{Binding SelectedCustomer.Phone}" />
						</Grid>
					</Grid>
				</Grid>

				<Grid Grid.Row="1">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="1.5*" MinWidth="120" />
						<ColumnDefinition Width="1.8*" MinWidth="120" />
						<ColumnDefinition Width="*" MaxWidth="90" />
						<ColumnDefinition Width="*" MaxWidth="80" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" MaxWidth="100" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" MaxWidth="85" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>

					<TextBlock Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=cbxCategory}" Text="{loc:Loc Turnovers.ProductType}" />
					<ComboBox
                x:Name="cbxCategory"
                Margin="1,3"
                FontSize="14"
                IsEditable="True"
                DisplayMemberPath="Name"
                Style="{StaticResource RoundedCombo}"
                ItemsSource="{Binding Categories}"
                SelectedItem="{Binding SelectedCategory}"
                Text="{Binding CategorySearchText, UpdateSourceTrigger=LostFocus}"
                utils:ComboBoxQuickNavBehavior.EnableQuickNavigation="True" />

					<TextBlock Grid.Column="1" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=cbxProduct}" Text="{loc:Loc Turnovers.ProductName}" />
					<ComboBox
                x:Name="cbxProduct"
                Grid.Column="1"
                Margin="1,3"
                FontSize="14"
                IsEditable="True"
                DisplayMemberPath="Name"
                Style="{StaticResource RoundedCombo}"
                ItemsSource="{Binding Products}"
                SelectedItem="{Binding SelectedProduct}"
                Text="{Binding ProductSearchText, UpdateSourceTrigger=LostFocus}"
                utils:ComboBoxQuickNavBehavior.EnableQuickNavigation="True" />

					<TextBlock Grid.Column="2" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=cbxLengthPerRoll}" Text="{loc:Loc Turnovers.RollLength}" />
					<ComboBox
                x:Name="cbxLengthPerRoll"
                Grid.Column="2"
                Margin="2,3"
                FontSize="14"
                IsEditable="True"
                DisplayMemberPath="LengthPerRoll"
                Style="{StaticResource RoundedCombo}"
                ItemsSource="{Binding WarehouseStocks}"
                SelectedItem="{Binding SelectedWarehouseStock}"
                Text="{Binding WarehouseStockSearchText, UpdateSourceTrigger=LostFocus}"
                utils:ComboBoxQuickNavBehavior.EnableQuickNavigation="True" />

					<TextBlock Grid.Column="3" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=txtRollCount}" Text="{loc:Loc Turnovers.RollCount}" />
					<TextBox
                x:Name="txtRollCount"
                Grid.Column="3"
                Margin="1,3"
                FontSize="14"
                Style="{StaticResource RoundedTextBox}"
                Validation.ErrorTemplate="{x:Null}"
                utils:NumericInput.Precision="0"
                utils:NumericInput.IsNumeric="True"
                utils:NumericInput.Value="{Binding CurrentItem.RollCount, UpdateSourceTrigger=LostFocus}" />

					<TextBlock Grid.Column="4" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=txtTotalLength}" Text="{loc:Loc Turnovers.TotalLength}" />
					<TextBox
                x:Name="txtTotalLength"
                Grid.Column="4"
                Margin="1,3"
                FontSize="14"
                Style="{StaticResource RoundedTextBox}"
                Validation.ErrorTemplate="{x:Null}"
                utils:NumericInput.IsNumeric="True"
                utils:NumericInput.Precision="2"
                utils:NumericInput.Value="{Binding CurrentItem.TotalLength, UpdateSourceTrigger=LostFocus}" />

					<TextBlock Grid.Column="5" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=txtUnitPrice}" Text="{loc:Loc Common.Price}" />
					<TextBox
                x:Name="txtUnitPrice"
                Grid.Column="5"
                Margin="1,3"
                FontSize="14"
                Style="{StaticResource RoundedTextBox}"
                Validation.ErrorTemplate="{x:Null}"
                utils:NumericInput.IsNumeric="True"
                utils:NumericInput.Precision="2"
                utils:NumericInput.Value="{Binding CurrentItem.UnitPrice, UpdateSourceTrigger=PropertyChanged}" />

					<TextBlock Grid.Column="6" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=txtTotalAmount}" Text="{loc:Loc Turnovers.TotalAmount}" />
					<TextBox
                x:Name="txtTotalAmount"
                Grid.Column="6"
                Margin="1,3"
                Style="{StaticResource RoundedTextBox}"
                Validation.ErrorTemplate="{x:Null}"
                FontSize="14"
                utils:NumericInput.IsNumeric="True"
                utils:NumericInput.Precision="2"
                utils:NumericInput.Value="{Binding CurrentItem.TotalAmount}" />

					<TextBlock Grid.Column="7" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=txtDiscountRate}" Text="{loc:Loc Turnovers.DiscountRate}" />
					<TextBox
                x:Name="txtDiscountRate"
                Grid.Column="7"
                Margin="1,3"
                FontSize="14"
                Style="{StaticResource RoundedTextBox}"
                Validation.ErrorTemplate="{x:Null}"
                utils:NumericInput.IsNumeric="True"
                utils:NumericInput.Precision="2"
                utils:NumericInput.Value="{Binding CurrentItem.DiscountRate}" />

					<TextBlock Grid.Column="8" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=txtDiscountAmount}" Text="{loc:Loc Turnovers.DiscountAmount}" />
					<TextBox
                x:Name="txtDiscountAmount"
                Grid.Column="8"
                Margin="1,3"
                FontSize="14"
                Style="{StaticResource RoundedTextBox}"
                Validation.ErrorTemplate="{x:Null}"
                utils:NumericInput.IsNumeric="True"
                utils:NumericInput.Precision="2"
                utils:NumericInput.Value="{Binding CurrentItem.DiscountAmount}" />

					<TextBlock Grid.Column="9" Panel.ZIndex="1" IsHitTestVisible="False" Style="{StaticResource FloatingLabelStyle}" Tag="{Binding ElementName=txtFinalAmount}" Text="{loc:Loc Turnovers.FinalAmount}" />
					<TextBox
                x:Name="txtFinalAmount"
                Grid.Column="9"
                Margin="1,3"
                Style="{StaticResource RoundedTextBox}"
                Validation.ErrorTemplate="{x:Null}"
                FontSize="14"
                utils:NumericInput.IsNumeric="True"
                utils:NumericInput.Precision="2"
                utils:NumericInput.Value="{Binding CurrentItem.FinalAmount}" />

					<Button  x:Name="btnAdd"
                Grid.Column="10"
                Margin="4,3"
                FontSize="14"
                Command="{Binding AddCommand}">
						<Button.Style>
							<Style TargetType="Button" BasedOn="{StaticResource RoundedButton}">
								<Setter Property="Background" Value="{DynamicResource Brand}" />
								<Setter Property="Foreground" Value="{DynamicResource OnBrand}" />
								<Setter Property="FontWeight" Value="SemiBold" />
								<Setter Property="Padding" Value="10,0" />
								<Setter Property="Content" Value="{loc:Loc Common.Add}" />
								<Style.Triggers>
									<DataTrigger Binding="{Binding IsEditing}" Value="True">
										<Setter Property="Content" Value="{loc:Loc Turnovers.Update}" />
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Button.Style>
					</Button>

					<Button x:Name="btnCancel"
                Grid.Column="11"
                Margin="4,3"
                FontSize="14"
                Content="{loc:Loc Common.Cancel}"
                Command="{Binding CancelEditCommand}"
                Background="{DynamicResource SurfaceAlt}"
                Foreground="{DynamicResource TextPrimary}"
                BorderBrush="{DynamicResource BorderStrong}"
                BorderThickness="1"
                Padding="10,0"
                Style="{StaticResource RoundedButton}"
                Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}" />
				</Grid>

				<DataGrid Grid.Row="2" Margin="0 5 0 0"
                  x:Name="dataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="True"
                  FontSize="14"
                  IsReadOnly="True"
                  ItemsSource="{Binding Sale.Items}"
                  HeadersVisibility="Column, Row"
                  RowHeaderWidth="40"
                  utils:DataGridRowNumbering.IsEnabled="True"
                  SelectionMode="Single"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
					<DataGrid.Columns>
						<DataGridTextColumn Width="1.5*" MinWidth="120" Binding="{Binding Product.Category.Name}" Header="{loc:Loc Turnovers.ProductType}" />
						<DataGridTextColumn Width="1.8*" MinWidth="120" Binding="{Binding Product.Name}" Header="{loc:Loc Turnovers.ProductName}" />
						<DataGridTextColumn Width="*" Binding="{Binding LengthPerRoll}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Turnovers.RollLength}" />
						<DataGridTextColumn Width="*" Binding="{Binding RollCount}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Turnovers.RollCount}" />
						<DataGridTextColumn Width="*" Binding="{Binding TotalLength, Converter={StaticResource ToStringConverter}}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Turnovers.TotalLength}" />
						<DataGridTextColumn Width="*" Binding="{Binding UnitPrice, Converter={StaticResource ToStringConverter}}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Common.Price}" />
						<DataGridTextColumn Width="*" Binding="{Binding TotalAmount, Converter={StaticResource ToStringConverter}}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Turnovers.TotalAmount}" />
						<DataGridTextColumn Width="*" Binding="{Binding DiscountRate, Converter={StaticResource ToStringConverter}}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Turnovers.DiscountRate}" />
						<DataGridTextColumn Width="*" Binding="{Binding DiscountAmount, Converter={StaticResource ToStringConverter}}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Turnovers.DiscountAmount}" />
						<DataGridTextColumn Width="*" Binding="{Binding FinalAmount, Converter={StaticResource ToStringConverter}}" ElementStyle="{StaticResource RightCellText}" Header="{loc:Loc Turnovers.FinalAmount}" />
					</DataGrid.Columns>
				</DataGrid>

				<StackPanel Grid.Row="4" Margin="0 10 0 0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
					<Button Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"
                    Content="{loc:Loc Common.Cancel}"
                    Style="{StaticResource SecondaryButton}" />

					<Button Command="{Binding SaveCommand}"
                    Content="{loc:Loc Common.Save}"
                    Style="{StaticResource PrimaryButton}" />
				</StackPanel>
			</Grid>
		</Border>

		<controls:BusyOverlay IsBusy="{Binding IsLoading}" Message="{loc:Loc Turnovers.Saving}" />
	</Grid>
</Page>
