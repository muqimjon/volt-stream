<Page
    x:Class="VoltStream.WPF.Payments.Views.PaymentEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:VoltStream.WPF.Commons.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="clr-namespace:VoltStream.WPF.Commons.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:VoltStream.WPF.Commons.Utils"
    Title="{loc:Loc Turnovers.PaymentEditTitle}"
    Background="{DynamicResource Canvas}"
    d:DesignHeight="450"
    d:DesignWidth="950"
    mc:Ignorable="d">

	<Grid>
		<Border Style="{StaticResource CardBorder}" Margin="10">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<!-- Yuqori qism -->
				<Grid Grid.Row="0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="350" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<!-- Chap tomon -->
					<Grid Grid.Column="0">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<Label Grid.Row="0" Content="{loc:Loc Turnovers.DateLabel}" VerticalAlignment="Center" FontSize="14" Margin="5,0,0,0" />
						<Label Grid.Row="1" Content="{loc:Loc Turnovers.CustomerLabel}" VerticalAlignment="Center" FontSize="14" Margin="5,0,0,0" />
						<Label Grid.Row="2" Content="{loc:Loc Turnovers.CurrencyTypeLabel}" VerticalAlignment="Center" FontSize="14" Margin="5,0,0,0" />
						<controls:UserCalendar
                        Grid.Column="1"
                        Width="150"
                        Margin="5,5,30,5"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        utils:EnterKeyTab.IsEnabled="True"
                        FontSize="14"
                        SelectedDate="{Binding Payment.PaidAt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />


						<ComboBox
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="5"
                    DisplayMemberPath="Name"
                    FontSize="14"
                    IsEditable="False"
                    IsEnabled="False"
                    Style="{StaticResource ModernCombo}"
                    ItemsSource="{Binding Customers}"
                    SelectedItem="{Binding Payment.Customer}" />

						<Grid Grid.Row="2" Grid.Column="1">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="70" />
							</Grid.ColumnDefinitions>

							<ComboBox
                        Margin="5"
                        DisplayMemberPath="Name"
                        FontSize="14"
                        IsEditable="False"
                        IsEnabled="False"
                        Style="{StaticResource ModernCombo}"
                        ItemsSource="{Binding Currencies}"
                        SelectedItem="{Binding Payment.Currency}" />

							<TextBlock
                        Grid.Column="1"
                        Margin="20,2,5,2"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Text="{loc:Loc Turnovers.RatePercent}" />

							<TextBox
                        Grid.Column="2"
                        Margin="5,2"
                        FontSize="14"
                        IsEnabled="{Binding Payment.Currency.IsEditable}"
                        Style="{StaticResource ModernTextBox}"
                        Text="{Binding Payment.ExchangeRate}"
                        utils:NumericInput.IsNumeric="True" />
						</Grid>
					</Grid>

					<!-- O'ng tomon -->
					<Grid Grid.Column="1">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="210" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<Grid Grid.Column="0">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>

							<Label Content="{loc:Loc Turnovers.AmountLabel}" FontSize="12" FontStyle="Italic" Foreground="{DynamicResource TextSecondary}" Margin="5,0,0,0" VerticalAlignment="Center" />
							<Label Grid.Row="1" Content="{loc:Loc Turnovers.IncomeLabel}" FontSize="14" Margin="5,0,0,0" VerticalAlignment="Center" />
							<Label Grid.Row="2" Content="{loc:Loc Turnovers.ExpenseLabel}" FontSize="14" Margin="5,0,0,0" VerticalAlignment="Center" />

							<TextBox
                        Grid.Column="1"
                        Margin="5,2"
                        FontSize="14"
                        Style="{StaticResource ReadOnlyValue}"
                        Text="{Binding Payment.Amount, StringFormat=N2}"
                        utils:NumericInput.IsNumeric="True" />

							<TextBox
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="5,2"
                        FontSize="14"
                        IsEnabled="{Binding Payment.IsIncomeEnabled}"
                        Style="{StaticResource ModernTextBox}"
                        Text="{Binding Payment.IncomeAmount}"
                        utils:NumericInput.IsNumeric="True" />

							<TextBox
                        Grid.Row="2"
                        Grid.Column="1"
                        Margin="5,2"
                        FontSize="14"
                        IsEnabled="{Binding Payment.IsExpenseEnabled}"
                        Style="{StaticResource ModernTextBox}"
                        Text="{Binding Payment.ExpenseAmount}"
                        utils:NumericInput.IsNumeric="True" />
						</Grid>

						<Grid Grid.Column="1">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>

							<Grid Grid.Row="0">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="Auto" />
								</Grid.ColumnDefinitions>

								<TextBlock Text="{loc:Loc Turnovers.BalanceLabel}" FontSize="12" FontStyle="Italic" Foreground="{DynamicResource TextSecondary}" Margin="5,0,0,0" VerticalAlignment="Center" />
								<TextBox
                            Grid.Column="1"
                            Margin="5,2"
                            FontSize="14"
                            Style="{StaticResource ReadOnlyValue}"
                            Text="{Binding BeginBalance, StringFormat=N2}"
                            utils:NumericInput.IsNumeric="True" />

								<TextBlock Grid.Column="2" Text="{loc:Loc Turnovers.ResultLabel}" FontSize="12" FontStyle="Italic" Foreground="{DynamicResource TextSecondary}" Margin="5,0,0,0" VerticalAlignment="Center" />
								<TextBox
                            Grid.Column="3"
                            Margin="5,2"
                            FontSize="14"
                            Style="{StaticResource ReadOnlyValue}"
                            Text="{Binding LastBalance, StringFormat=N2}"
                            utils:NumericInput.IsNumeric="True" />
							</Grid>

							<Grid Grid.Row="1">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="60" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>

								<Label Content="{loc:Loc Turnovers.NoteLabel}" FontSize="14" Margin="5,0,11,0" VerticalAlignment="Center" />
								<TextBox
                            Grid.Column="1"
                            Margin="5,2"
                            FontSize="14"
                            Style="{StaticResource ModernTextBox}"
                            Text="{Binding Payment.Description}" />
							</Grid>
						</Grid>
					</Grid>
				</Grid>

				<!-- Pastki qism - tugmalar -->
				<Grid Grid.Row="2">
					<StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
						<Button
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"
                    Content="{loc:Loc Common.Cancel}"
                    Style="{StaticResource SecondaryButton}" />

						<Button
                    Command="{Binding SaveCommand}"
                    Content="{loc:Loc Common.Save}"
                    Style="{StaticResource PrimaryButton}" />
					</StackPanel>
				</Grid>
			</Grid>
		</Border>

		<controls:BusyOverlay IsBusy="{Binding IsLoading}" Message="{loc:Loc Turnovers.Saving}" />
	</Grid>
</Page>
