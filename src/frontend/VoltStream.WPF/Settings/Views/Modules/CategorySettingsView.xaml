<UserControl x:Class="VoltStream.WPF.Settings.Views.Modules.CategorySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:uc="clr-namespace:VoltStream.WPF.Commons.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="20" Background="{DynamicResource Canvas}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="300" />
		</Grid.ColumnDefinitions>

		<!-- List -->
		<DataGrid Grid.Column="0"
                  Margin="0,0,20,0"
                  ItemsSource="{Binding Categories}"
                  IsReadOnly="True">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" ElementStyle="{StaticResource CenterCellText}" />
				<DataGridTextColumn Header="Nomi" Binding="{Binding Name}" Width="*" />
				<DataGridTemplateColumn Header="Amallar" Width="100">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
								<Button Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource EditActionButton}"
                                        ToolTip="Tahrirlash">
									<fa:IconImage Icon="Pen" Foreground="{DynamicResource Warning}" Width="16" Height="16" />
								</Button>
								<Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource DeleteActionButton}"
                                        ToolTip="O'chirish">
									<fa:IconImage Icon="Trash" Foreground="{DynamicResource Danger}" Width="16" Height="16" />
								</Button>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<!-- Sidebar form -->
		<Border Grid.Column="1"
                Style="{StaticResource CardBorder}"
                VerticalAlignment="Top">
			<StackPanel>
				<ContentControl Content="{Binding IsEditing}">
					<ContentControl.Style>
						<Style TargetType="ContentControl">
							<Setter Property="ContentTemplate">
								<Setter.Value>
									<DataTemplate>
										<TextBlock Text="Yangi qo'shish" Style="{StaticResource SubtitleText}" Margin="0,0,0,15" />
									</DataTemplate>
								</Setter.Value>
							</Setter>
							<Style.Triggers>
								<DataTrigger Binding="{Binding}" Value="True">
									<Setter Property="ContentTemplate">
										<Setter.Value>
											<DataTemplate>
												<TextBlock Text="Tahrirlash" Style="{StaticResource SubtitleText}" Margin="0,0,0,15" />
											</DataTemplate>
										</Setter.Value>
									</Setter>
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</ContentControl.Style>
				</ContentControl>

				<TextBlock Text="Nomi" Style="{StaticResource FieldLabel}" />
				<TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBox}" />

				<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
					<Button Content="Bekor qilish"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding CancelCommand}"
                            Margin="0,0,10,0"
                            Width="100" />

					<Button Content="Saqlash"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding SaveCommand}"
                            Width="100" />
				</StackPanel>
			</StackPanel>
		</Border>

		<uc:BusyOverlay Grid.ColumnSpan="2"
                        IsBusy="{Binding IsLoading}"
                        Message="Yuklanmoqda..." />
	</Grid>
</UserControl>
