<UserControl x:Class="VoltStream.WPF.Settings.Views.Modules.ConnectionSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20" Background="{DynamicResource Canvas}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Status -->
        <StackPanel Grid.Row="0"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,20">
            <TextBlock Text="{Binding Url}"
                       Foreground="{DynamicResource TextPrimary}"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       FontSize="{DynamicResource FontSizeSubtitle}" />
            <TextBlock Text="{Binding StatusText}"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"
                       FontWeight="SemiBold">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="Connected">
                                <Setter Property="Foreground" Value="{DynamicResource Success}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="Disconnected">
                                <Setter Property="Foreground" Value="{DynamicResource Danger}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- Form -->
        <StackPanel Grid.Row="1"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Width="320"
                    Margin="0,0,0,20"
                    Orientation="Vertical">

            <!-- URL konfiguratsiyasi -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Server manzili" Style="{StaticResource SubtitleText}" Margin="0,0,0,12" />

                    <CheckBox Content="HTTPS ishlatilsin"
                              Style="{StaticResource ModernCheck}"
                              IsChecked="{Binding IsHttps}"
                              Margin="0,0,0,12" />

                    <Grid Margin="0,0,0,10">
                        <TextBox x:Name="HostTextBox"
                                 Style="{StaticResource ModernTextBox}"
                                 Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Text="Host (masalan: localhost)"
                                   Foreground="{DynamicResource TextTertiary}"
                                   Margin="12,0,0,0"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding Text, ElementName=HostTextBox, Converter={StaticResource EmptyToVisibleConverter}}" />
                    </Grid>

                    <Grid>
                        <TextBox x:Name="PortTextBox"
                                 Style="{StaticResource ModernTextBox}"
                                 Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Text="Port (masalan: 5000)"
                                   Foreground="{DynamicResource TextTertiary}"
                                   Margin="12,0,0,0"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding Text, ElementName=PortTextBox, Converter={StaticResource EmptyToVisibleConverter}}" />
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0">
            <Button Content="Tekshirish"
                    Style="{StaticResource SecondaryButton}"
                    Command="{Binding TestCommand}"
                    Width="140"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolToBoolConverter}}"
                    Margin="0,0,10,0" />
            <Button Content="Manzilni saqlash"
                    Style="{StaticResource SuccessButton}"
                    Command="{Binding SaveCommand}"
                    Width="160"
                    ToolTip="Host, port va HTTPS asosida URLni yaratadi" />
        </StackPanel>
    </Grid>
</UserControl>
